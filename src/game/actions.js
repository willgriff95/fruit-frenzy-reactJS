// export const addToCartStarted = createAction('Add to cart started', [
//     'productId'
// ])


export const geTest = () => (dispatch, getState) => {
    // console.log(dispatch, getState)
    debugger
}